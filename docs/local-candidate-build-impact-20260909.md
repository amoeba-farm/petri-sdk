# STEP 1 SDK local candidate dependency impact

Build only: production compiler emission and artifact construction are authorized. Tests, typecheck-only commands, proofs, simulations, package/checker suites, helper execution, publication and live writes are held.

The authorized browser packaging follow-up adds `npm run build:wallet-browser` after TypeScript emission. It bundles the complete exported wallet entry into `dist/browser-bundle/wallet.js` with the installed esbuild browser resolver and emits its dependency metafile. It does not execute the bundle, externalize Node builtins, or invoke the wallet qualification checker. Both emitted files are included by the existing package file allowlist and recorded in the artifact receipt.

Inspected before mutation: package.json scripts/files/exports/dependencies/bundledDependencies, npm lock, Cargo manifest/lock, tsconfig production emission, scripts/run-npm.mjs, existing release updater/finalizer, .gitignore and hidden .github CI mirror. No prepare/prepack/install hook is declared in the SDK. prepublishOnly invokes release qualification and will not run. npm installation and packing use --ignore-scripts; production build uses explicit npm run build. Existing updater scripts describe immutable deployed source and would overwrite live truth, so they are inapplicable to this uncommitted candidate.

Candidate changes: package.json file dependency + packaged vendor allowlist, package-lock.json actual resolved tarball integrity, Cargo.toml actual relative native source path + Cargo.lock generated path resolution, package-owned candidate tgz and .gitignore exception, release/local-candidate-build.v1.json + full source inventory, Node/browser/Rust build identity export, digest mismatch rejection in current governed admission. A reusable candidate updater will bind actual file bytes; final artifact receipt lives under artifacts/sdk.

Retained historical release evidence: vendor/GOVERNED_RUNTIME_PROVENANCE.json and PROVENANCE.json, release/current-deployment.v1.json, spread-scoped-final-20260909.json, release-train.v1.json, scoped-build-attestation-20260909.json, src/protocol/release-train.ts and identity constants, .github/workflows/ci.yml released Git mirror, historical release/checker/fixture expected pins. These still describe the observed deployed release and cannot truthfully adopt a made-up Git SHA for local source. Candidate identity is separate and cannot authorize changed ABI against those receipts. Future qualification must explicitly reconcile this set; no held checker is run or made to pass by replacing expected hashes.

Consumers: npm tgz with package-owned native bundles and ordinary npm dependency installation; Rust local SDK source and actual native path. No pnpm shared-store hydration. A source inventory digest is not a Git commit, deployed program hash or qualification result.
