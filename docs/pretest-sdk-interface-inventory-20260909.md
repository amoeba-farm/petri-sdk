# SDK pre-test coverage and shared interfaces

Status: earlier pre-test source handoff is preserved. Subsequent writer-DLMM replacement source is tracked in [writer-dlmm-replacement-interfaces-20260909.md](writer-dlmm-replacement-interfaces-20260909.md); that document is authoritative for its current interfaces and residual dependencies. No tests, typechecks, builds, package checkers, application flows, simulations, signing, transactions, commits, publication or deployment were run. Earlier syntax-only and whitespace results apply only to the earlier source inputs. They do not cover the subsequent writer-DLMM changes, which have not been parsed or checked. Neither prior check establishes behavioral or type correctness.

Only the existing SDK, Spread and Lean tasks participate. Spread owns native semantics and helper source; SDK owns portable plans, read evidence, client/wallet/Rust validation and native invocation; Lean owns independent semantic admission, retained prepare/submit/confirmation and service composition. Parent owns whole-list completion. The accepted plan is `3943/ameba_spread/docs/governance/pretest-blocker-implementation-plan-20260909.md`.

## Implemented source paths

| Item | Concrete SDK source and peer contract | Remaining prerequisites / scope |
| --- | --- | --- |
| Auction refund | `src/protocol/writer-operation.ts`: `auction_refund {owner,auction,bid}`, exact tag235/native manifest/semantic binding. `src/client.ts`: `writerOperations.prepareRefund` -> `/dlmm/writer-sleeves/refunds/prepare`; exact raw envelope/digest/local-intent decoding. `src/wallet/{intent,plans,operations,writer-accounts}.ts`: finalized bid/index/auction/actor, immutable bidder destination and net refundable amount. `rust/src/{writer_sleeve,writer_operation}.rs`: exact builder reconstruction and setup validation. Lean `writer-funds-authority.mjs` supplies exact custody and admission. | Early cancellation needs bidder and active bidding/deadline; later refunds permit crank to the stored destination. Net escrow minus premium, fee and prior refunds must be positive. No caller-directed recipient or amount. |
| Principal withdrawal | Distinct `withdraw_principal {owner,sleeve,amountAtoms}`, tag228 in the same Node/client/wallet/Rust paths; `prepareWithdrawal` -> `/dlmm/writer-sleeves/withdrawals/prepare`. Native 14-account shape, exact 1:1 owner Flat burn and sleeve USDC output. | Funding, no active auction/close, zero encumbrances and sufficient physical/accounted principal/Flat. Separate from collateral withdrawal and close finalization. |
| Writer output/input prerequisites | `currentWriterClassicOutputSetupInstructionBatches`, `classicOutput.setupFacts` and schema2 `classic_output_create` or `cold_load` plus `classicOutputSetupFacts`. Same-transaction canonical classic ATA create follows authenticated Light load where required. Wallet and Rust derive the six-meta ATA instruction and validate exact signer/destination/setup commitments. `close_finalize` also permits this classic output setup at native destination8/mint9/owner0, preserving its exact 13-account prefix and series triples. | Finalized absent/system-empty classic destination; existing valid output preserves native identity. Genuine spendable funds and authentic cold witnesses are required. No implicit funding. |
| Stake queue | `current-oracle-public.ts` and `current-oracle.ts` request `queue_stake_amba_for_samba {ambaAmountAtomic}`; `current-oracle-staking.ts` finalized ledger/config/pool/activation frame and native `planCurrentOracleStakingOperationV1`; planner/client/adapter tag138 public Oracle transport. | Existing owner AMBA ledger funds; exact native activation state. AMBA is separately bound from USDC. |
| Stake activate | `activate_queued_stake_amba_for_samba {minSambaOutAtomic}`, tag139; exact native ratio/clock/funnel/governance math. `current-oracle-staking-setup.ts` independently derives canonical sAMBA ATA. Adapter/client exact main sequence is compute600000, ATA idempotent, tag139; setup arrays remain empty, one action transaction. | Absent/system-empty output allowed and created atomically; activation delay, empty reward funnel, supply/share bounds and native governance lock remain. |
| Unstake request | `request_unstake_samba {sambaAmountAtomic,minAmbaOutAtomic}`, tag131, current native balance/supply/custody/state planning and public Oracle request/manifest/transaction decoding. | Existing canonical owner sAMBA source, exact amount/slippage and governance lock. |
| Unstake complete | `complete_unstake_samba {}`, tag133, exact finalized request/pool clock and native completion plan. | Native cooldown604800 seconds and requested amount; native handler may create absent player ledger atomically. |
| Staking evidence | `proofFacts.stakingEvidenceJson` binds `{readWindowStartSlot,readWindowEndSlot,currentUnixTimestamp,accountFacts}` from the SAME preparation read. Facts carry address/owner/executable/full base64/data SHA/observed slot. Adapter and client commitments include the full frame. | Lean independently decodes exact raw fields for economics. No metadata-only verified boolean replaces admission. |
| Oracle dispute commit/reveal | User resolved source/opening/update Oracle voting. Existing tag176/177 planners validate exact dispute/vote/pot/snapshot/custody/windows/choice and native commitment. Public request redaction and client mapping retained. New `disputeEvidenceJson` captures same-plan finalized raw dispute/pot/vote/pool/mint/owner-token/anchor bytes and economic `currentSlot`. | Explicit choice/salt, correct owner and time window; create-only vote absence is preserved. Lean independently consumes raw frame. No invented writer dispute system. |
| Seat approval | Bounded native/spec discovery found no identified seat action beyond a wallet label and governance council references. | Product identity remains with parent/user. No tag, alias, request or authority was invented. |
| Owner bounty entitlement/receipt | `current-oracle-reward-evidence.ts`: per-subject receipt-first reads, source/support/retained merge lineage, accepted opening/finalized update and schedule/pool inputs; exact receipt81 layout. Shared reward-schedule decoder corrected against native `state/rewards.rs` to URS160/version2: update units117, total121/remaining129, last slot137, escrow145, fee total149, fee-sweep bool157 and zero tail158..159; removed stale active-weight-manifest field. Two existing fixture encoders were source-aligned without execution. Pool proposer bps are1..10000; update registration units are exactly1 or3. Finalized UC2 requires positive states/time, nonzero evidence/archive hashes, multiplier1|3 and reveal slot inside its native window. Existing update-reveal admission corrected to initial multiplier1 (native committed shape); matching fixture source adjusted without execution. Shared read-only `readCurrentOracleRewardEntitlementInputs` contains no transaction construction or implicit collateral initialization. All compact reads now use native-verified current root/queue evidence. | Actual entitlement and canonical payout balances remain distinct. Claimable here means positive entitlement; actual claim preparation also checks collateral and payout capacity. Missing/unavailable evidence is never zero. |
| Owner bounty discovery | `readCurrentOracleOwnerBountyInventory` facade derives/rereads canonical Market/Month/Vault and returns `marketAddress:string` and `oracleMonthAddress:string` alongside raw `anchorAccounts`, allowing Lean to independently validate canonical series/PDA without guessing row identities; `readCurrentOracleOwnerRewardInventory` combines caller-authenticated source IDs with exact retained-merge/update GPA identities, validates native layouts/PDA, sorts/deduplicates candidates and returns `cursor`, `limit` default16/max64, `candidateCount`, `nextCursor`, paged candidates/evidence. GPA row bytes are reused for local decoding. | Explicit supplied registry scope and per-query slot evidence; `globalInventoryComplete:false` prevents false global completeness. Optional `includeNativeAccountSnapshots:false` is propagated from the facade through inventory and per-subject collection. After each successful SDK native check and before caching/collecting evidence, native request `accounts` are omitted and `verificationEvidence.nativeAccountSnapshotsIncluded:false` marks the non-replayable provenance. Full leaf, original Photon statement/proof, native result (including exact original request SHA), context binding and executable SHA remain. Default true preserves the full existing shape. Lean reconstructs fresh accounts for independent verification and owns authoritative complete coverage. |
| Bounty verification | `current-photon-verified.ts` validates exact provider topology, namespace/domain/PDA/Light address, discriminator and SHA256BE full envelope; a single finalized standard-RPC snapshot supplies tree+queue bytes. The actual Spread native helper recomputes full leaf hash, verifies current root/Groth16+bloom or current queue inclusion+bloom. `current-compressed-evidence-verifier.ts` checks actual executable SHA, bounded shell-free credential-free invocation, exact stdin/result/context bindings. | No built helper is claimed. An actual qualified native package/helper executable and deployment-owned path/SHA are required. No provider assertion or copied `verified:true` is authoritative. Lean must rebuild/reverify independently. |
| Raw Photon transport | Existing provider statement APIs are explicitly `authorized_photon_provider`, `onchain_at_execution`, `finalizedRootVerified:false` with exact proof bytes and statement committed to witness. New `observeCurrentVerifiedCompressedStateEvidence` is separate and never falls back. | Raw provider evidence alone does not satisfy Lean ORACLE-BOUNTY-001. Queue mode does not pretend to be a Groth16/root proof. |
| LP adds/removals | Existing integrated native layout/builders preserved. Spread agreed adds Pending/Active/Paused, removals also Settled, matching position/page init; no new ABI. Shared V3 readiness source removed global pause suppression and retains pause as observation. | Exact operation authorization, reserves, custody, share/slippage/physical deltas and settlement rules remain in native/Lean admission. Generic readiness does not establish trade eligibility. |
| Collateral initialize/deposit/withdraw | Existing explicit `CurrentPositionActionRequest`, finalized prepare context and exact signed-plan observation/manifest preserved. Removed two SDK vault-pause-only blocks in alignment with public native tags10/11. Init9 remains explicit. `readCurrentUserCollateral` now accepts `minimumContextSlot`, reads finalized context and returns `observedAtSlot` for present AND absent account; factory/DTO propagate it. | Lean uses finalized signature slot for read-only refresh/reconciliation and retains original inspection/admission/ALT context. No hidden init or resend; expired/unobserved old attempt remains untouched. |
| Classic deposit/oracle funding | Existing canonical SPL source/vault mint/owner/layout/initialization/frozen/native/balance checks and precise prerequisite errors retained. Oracle funding tag155 uses USDC; staking uses AMBA. | Actual classic spendable balance is operational. Compressed/ledger balances cannot be substituted for SPL funds. |
| Long/Flat claims, writer deposit/bid/close | Settlement collective246 (20 accounts/input7/mint6) and Flat247 (17 accounts/input4/mint3) now accept existing `coldAccount` setup with exact u64 claim amount at payload1 across Node/wallet/Rust. Wallet permits canonical absent/system-empty Light USDC output where the native claim creates it atomically; existing outputs retain custody validation. Other exact portable/native/wallet paths retained. SDK generic runtime no longer adds blanket pause gate; peer matrix removes native pause-only suppression on Funding deposit/principal exit, complete close and finalized claims. | Explicit stages, current release identity, complete basket, ownership, solvency, payout snapshot, accounting and applicable cooldowns remain. |
| Flat transfer / close begin | Existing exact owner/recipient/amount and finalized close stage sources preserved, including cold input setup. | No recipient inference or execution claim; native cooldown and actual funds remain. |
| Expiry authorization | Existing `current-position-expiry.ts` bounded finalized planning and wallet `expiry-authorization.ts` authorization remain available; service persistence/execution belongs to Lean and consumes exact planned records/builders. | SDK read projection is explicitly planning-only; its local execution flag cannot become a service-wide arbitrary disable. Required credentials/resources and objective settlement/authority rules remain. Lean confirmed its private authority persists authorization using DB and keeper identity/address/KMS resources, with availability derived from resources and exact ABI. No SDK service-authority claim is needed. |
| Median/membership/Opening | Preserved `current-oracle-membership{,-internal}.ts`, native tag200/source-index ABI209/366, reverse-prefix resume and `operator/current-oracle-opening.ts` facade and source export inventories. | Real native artifact and source ABI qualification needed; old admitted release tags/hashes were not fabricated or repinned. |

## Native helper source and artifact boundary

Spread owns `tools/current-compressed-evidence-verifier/{Cargo.toml,Cargo.lock,INTERFACE.md,source.json,src/main.rs}` and its exact native package export `@amoeba/spread-release-tools/compressed-evidence-verifier-source`. SDK `readCurrentCompressedEvidenceVerifierSource` locates and fingerprints the source allowlist without build or execution. SDK package checker source requires the helper source inventory plus the new transport/read modules. The helper uses the actual pinned Light verifier/root/queue primitives; no new onchain instruction or changed dependency pin was introduced.

Before later qualification: build/typecheck the complete coherent native+SDK+Lean sources, resolve the helper standalone seeded Cargo lock, produce the real native package and platform helper, record its actual executable SHA in deployment-owned configuration, configure `createCurrentCompressedEvidenceVerifier` and `createCurrentPhotonConnection({compressedEvidenceVerifier})`, and perform the authorized component checks. Old package measured size limits, release receipts, pins, generated outputs and archived checksums remain unchanged because no real new artifact was measured. No such qualification work was executed in this pass.

Lean acknowledged the finalized owner-bounty facade and full verified evidence schema: its current-compressed-root-authority.mjs independently rebuilds the native request, reads fresh finalized tree/queue accounts, binds owner request and identity, invokes the configured native verifier, compares result fields, and strips large proof/tree data. Lean also confirmed classicOutput integration in its finalizer/funds admission and the expiry projection/service boundary. A subsequent concrete resource concern is addressed by the optional snapshot omission described above; it prevents per-candidate tree/queue snapshot retention when Lean independently reads them again. These are peer source acknowledgments, not runtime verification. SDK source handoff has no outstanding reported consumer discrepancy; future concrete discrepancies may require follow-up source edits. The seat operation is still unidentified and has not been implemented. Parent owns overall completion and later test/release authorization.

## Final Lean source acknowledgment

Lean confirmed `writer-claim-authority` supplies exact owner/mint/requested-amount `required_hot_input` for collective and Flat claims, and `writer-close-next-authority` supplies canonical `classicOutput` for selected finalization. The finalizer observes setup accounts and passes the same portable shape. Lean aligned URS160/v2, proposer bps1..10000, update units1|3 and finalized UC2 checks; its public owner authority sets `includeNativeAccountSnapshots:false`, consumes explicit market/month addresses and reconstructs fresh native verifier inputs instead of replaying omitted-account requests. Lean reports no further SDK changes pending. Runtime and proof qualification remain deferred.
