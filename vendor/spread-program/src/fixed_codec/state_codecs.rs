use super::fixed_state_deserialize;
use super::*;

fixed_state_deserialize!(OracleRecipeSourceIndex, OracleRecipeSourceIndex::LEN, {
    is_initialized: bool,
    bump: u8,
    account_discriminator: [u8; 3],
    account_version: u8,
    month: Pubkey,
    recipe_hash: [u8; 32],
    manifest_hash: [u8; 32],
    remaining_hash: [u8; 32],
    expected_source_count: u16,
    expected_bucket_count: u16,
    remaining_source_count: u16,
    indexed_bucket_count: u16,
    indexed_bucket_weight_bps: u16,
    last_bucket_id: [u8; 32],
    last_source_id: [u8; 32],
    complete: bool,
});

fixed_state_deserialize!(OracleBucketSourceIndex, OracleBucketSourceIndex::LEN, {
    is_initialized: bool,
    bump: u8,
    account_discriminator: [u8; 3],
    account_version: u8,
    month: Pubkey,
    recipe_hash: [u8; 32],
    bucket_id: [u8; 32],
    group_index: u16,
    first_source_index: u16,
    bucket_weight_bps: u16,
    source_count: u16,
    source_ids: [[u8; 32]; crate::constants::MAX_ORACLE_BUCKET_SOURCES],
});

fixed_state_deserialize!(VaultConfig, 135, {
    is_initialized: bool,
    bump: u8,
    admin: Pubkey,
    oracle_authority: Pubkey,
    usdc_mint: Pubkey,
    vault_token_account: Pubkey,
    paused: bool,
    account_discriminator: [u8; 3],
    account_version: u8,
});

fixed_state_deserialize!(OracleEmergencyDisputeV3, 326, {
    is_initialized: bool,
    bump: u8,
    account_discriminator: [u8; 3],
    account_version: u8,
    month: Pubkey,
    dispute_id: [u8; 32],
    kind: OracleEmergencyDisputeKind,
    target_id: [u8; 32],
    target_account: Pubkey,
    opened_by: Pubkey,
    status: OracleChallengeStatus,
    fallback_choice: u8,
    resolved_choice: u8,
    snapshot_slot: u64,
    snapshot_total_major_tokens: u64,
    committed_power: u64,
    committed_vote_count: u32,
    revealed_power: u64,
    revealed_vote_count: u32,
    winning_power: u64,
    winning_vote_count: u32,
    winning_choice: u8,
    choice_power: [u64; 3],
    choice_vote_count: [u32; 3],
    supermajority_bps: u16,
    commit_deadline_slot: u64,
    reveal_deadline_slot: u64,
    pot: Pubkey,
    resolved_slot: u64,
    minimum_vote_amount: u64,
    choice_count: u8,
});

fixed_state_deserialize!(OracleBucketMedianState, 245, {
    is_initialized: bool,
    bump: u8,
    account_discriminator: [u8; 3],
    account_version: u8,
    month: Pubkey,
    bucket_id: [u8; 32],
    group_index: u16,
    bucket_weight_start_bps: u16,
    bucket_weight_bps: u16,
    frozen_source_count: u16,
    active_source_count: u16,
    eligible_source_count: u16,
    status: OracleBucketMedianStatus,
    bucket_delta_bps: i64,
    last_recomputed_ts: u64,
    source_snapshot_hash: [u8; 32],
    recompute_processed_source_count: u16,
    last_recompute_source_id: [u8; 32],
    emergency_snapshot_slot: u64,
    emergency_snapshot_total_samba: u64,
    opening_source_deltas_bps: [i64; crate::constants::MAX_ORACLE_BUCKET_SOURCES],
});

fixed_state_deserialize!(OracleActiveWeightManifest, 159, {
    is_initialized: bool,
    bump: u8,
    account_discriminator: [u8; 3],
    account_version: u8,
    month: Pubkey,
    phase: OracleRecipeWeightPhase,
    expected_source_count: u16,
    expected_group_count: u16,
    processed_source_count: u16,
    processed_group_count: u16,
    current_group_id: [u8; 32],
    current_group_source_count: u16,
    current_group_active_count: u16,
    current_group_bucket_weight_bps: u16,
    processed_bucket_weight_bps: u16,
    last_collected_source_id: [u8; 32],
    rolling_manifest_hash: [u8; 32],
    max_open_interest_payout: u64,
});

fixed_state_deserialize!(OracleSettlementSourceManifest, 113, {
    is_initialized: bool,
    bump: u8,
    account_discriminator: [u8; 3],
    account_version: u8,
    month: Pubkey,
    phase: OracleRecipeWeightPhase,
    expected_source_count: u16,
    expected_bucket_count: u16,
    processed_source_count: u16,
    processed_bucket_count: u16,
    declared_weight_total_bps: u16,
    current_bucket_id: [u8; 32],
    rolling_source_digest: [u8; 32],
});

fixed_state_deserialize!(OracleRecipeWeightManifest, 181, {
    is_initialized: bool,
    bump: u8,
    account_discriminator: [u8; 3],
    account_version: u8,
    month: Pubkey,
    phase: OracleRecipeWeightPhase,
    expected_source_count: u16,
    expected_bucket_count: u16,
    recipe_hash: [u8; 32],
    rolling_manifest_hash: [u8; 32],
    current_bucket_id: [u8; 32],
    current_bucket_weight_bps: u16,
    current_bucket_source_count: u16,
    processed_source_count: u16,
    processed_bucket_count: u16,
    declared_weight_total_bps: u16,
    last_collected_source_id: [u8; 32],
});

fixed_state_deserialize!(OracleSambaEmergencyPot, 257, {
    is_initialized: bool,
    bump: u8,
    account_discriminator: [u8; 3],
    account_version: u8,
    dispute: Pubkey,
    month: Pubkey,
    samba_mint: Pubkey,
    token_account: Pubkey,
    payout_mode: OracleSambaEmergencyPayoutMode,
    total_committed: u64,
    committed_vote_count: u32,
    winning_choice: u8,
    winning_power: u64,
    winning_vote_count: u32,
    registered_power: u64,
    registered_vote_count: u32,
    registered_base_total: u64,
    dust_recipient_vote: Pubkey,
    dust_amount: u64,
    registration_finalized: bool,
    remaining_liability: u64,
    settled_vote_count: u32,
    total_paid: u64,
    resolved_slot: u64,
    last_updated_slot: u64,
});

fixed_state_deserialize!(OracleSourceState, 335, {
    is_initialized: bool,
    bump: u8,
    month: Pubkey,
    source_id: [u8; 32],
    bucket_id: [u8; 32],
    source_type_hash: [u8; 32],
    canonical_locator_hash: [u8; 32],
    source_definition_hash: [u8; 32],
    proposer: Pubkey,
    baseline_state: u64,
    current_state: u64,
    listing_bond_locked: u64,
    support_stake_total: u64,
    bucket_weight_bps: u16,
    status: OracleSourceStatus,
    opening_submitted: bool,
    opening_evidence_hash: [u8; 32],
    last_finalized_step: u64,
    observation_count: u8,
    rolling_observation_hash: [u8; 32],
});

fixed_state_deserialize!(OracleSourceObservations, 582, {
    is_initialized: bool,
    bump: u8,
    account_discriminator: [u8; 3],
    account_version: u8,
    month: Pubkey,
    source: Pubkey,
    states: [u64; crate::constants::MAX_ORACLE_SOURCE_OBSERVATIONS],
    source_times: [u64; crate::constants::MAX_ORACLE_SOURCE_OBSERVATIONS],
});

fixed_state_deserialize!(OracleSourceChallenge, 295, {
    is_initialized: bool,
    bump: u8,
    month: Pubkey,
    challenge_id: [u8; 32],
    source: Pubkey,
    source_id: [u8; 32],
    comparison_source: Pubkey,
    comparison_source_id: [u8; 32],
    challenger: Pubkey,
    reason: u8,
    bond: u64,
    required_bond: u64,
    status: OracleChallengeStatus,
    evidence_hash: [u8; 32],
    rule_review_slot: u64,
    escrow_disposition: OracleEscrowDisposition,
    emergency_snapshot_total_major_tokens: u64,
    emergency_snapshot_version: u8,
    failed_schedule_escrow_counted: bool,
});

fixed_state_deserialize!(OracleUsdcSourceReward, 278, {
    is_initialized: bool,
    bump: u8,
    account_discriminator: [u8; 3],
    account_version: u8,
    month: Pubkey,
    schedule: Pubkey,
    sku_pool: Pubkey,
    source: Pubkey,
    source_id: [u8; 32],
    proposer: Pubkey,
    supporter_count: u32,
    registered: bool,
    terminal_status: OracleSourceStatus,
    opening_claim: Pubkey,
    last_updated_slot: u64,
    merged_into_source: Pubkey,
    max_merge_depth: u8,
    listing_escrow_counted: bool,
});

fixed_state_deserialize!(OracleUsdcSkuPool, 226, {
    is_initialized: bool,
    bump: u8,
    account_discriminator: [u8; 3],
    account_version: u8,
    schedule: Pubkey,
    month: Pubkey,
    bucket_id: [u8; 32],
    source_reward_budget: u64,
    remaining_source_reward_budget: u64,
    opening_reward_budget: u64,
    remaining_opening_reward_budget: u64,
    update_reward_budget: u64,
    remaining_update_reward_budget: u64,
    proposer_reward_bps: u16,
    listing_bond: u64,
    support_bond: u64,
    opening_bond: u64,
    update_min_bond: u64,
    challenge_min_bond: u64,
    challenge_max_bond: u64,
    challenge_bond_bps: u16,
    registered_source_count: u32,
    registered_opening_count: u32,
    registered_update_count: u32,
    registered_update_reward_units: u32,
    last_updated_slot: u64,
});

fixed_state_deserialize!(OracleUsdcRewardSchedule, 158, {
    is_initialized: bool,
    bump: u8,
    account_discriminator: [u8; 3],
    account_version: u8,
    month: Pubkey,
    authority: Pubkey,
    reward_vault: Pubkey,
    phase: OracleUsdcRewardSchedulePhase,
    sku_pool_count: u16,
    registered_source_count: u32,
    registered_opening_count: u32,
    registered_update_count: u32,
    registered_update_reward_units: u32,
    total_reward_budget: u64,
    remaining_reward_budget: u64,
    last_updated_slot: u64,
    outstanding_prelisting_escrow_count: u32,
    trading_fee_bounty_total: u64,
    bounty_fee_sweep_finalized: bool,
});

fixed_state_deserialize!(OracleEconomicParams, 18, {
    emergency_supermajority_bps: u16,
    emergency_commit_window_slots: u64,
    emergency_reveal_window_slots: u64,
});

fixed_state_deserialize!(WriterPolicyRegistryV1, 198, {
    is_initialized: bool,
    bump: u8,
    account_discriminator: [u8; 3],
    account_version: u8,
    vault_config: Pubkey,
    policy_authority: Pubkey,
    pending_policy_authority: Pubkey,
    protocol_fee_vault: Pubkey,
    pending_activation_slot: u64,
    rotation_delay_slots: u64,
    latest_policy_version: u64,
    last_updated_slot: u64,
    reserved: [u8; 32],
});

fixed_state_deserialize!(WriterPolicySnapshotV1, 344, {
    is_initialized: bool,
    bump: u8,
    account_discriminator: [u8; 3],
    account_version: u8,
    sleeve: Pubkey,
    registry: Pubkey,
    policy_version: u64,
    regime_input_version: u64,
    policy_hash: [u8; 32],
    scenario_set_hash: [u8; 32],
    risk_limit_hash: [u8; 32],
    series_family_hash: [u8; 32],
    security_mode: WriterSecurityMode,
    reserve_rounding_mode: WriterReserveRoundingMode,
    auction_priority_rule: WriterAuctionPriorityRule,
    v2_feature_flags: u8,
    max_series: u8,
    reserved_0: u8,
    primary_fee_bps: u16,
    reserved_1: [u8; 2],
    drawdown_scale: u64,
    worst_drawdown_limit: u64,
    upper_drawdown_limit: u64,
    lower_drawdown_limit: u64,
    lower_tail_max_settlement_atomic: u64,
    upper_tail_min_settlement_atomic: u64,
    operational_buffer_atoms: u64,
    max_auction_issue_atoms: u64,
    max_close_flat_atoms: u64,
    created_slot: u64,
    sealed_slot: u64,
    reserved: [u8; 32],
});

fixed_state_deserialize!(WriterSettlementGroupV1, 558, {
    is_initialized: bool,
    bump: u8,
    account_discriminator: [u8; 3],
    account_version: u8,
    underlying_id: [u8; 32],
    expiry_ts: u64,
    settlement_mint: Pubkey,
    anchor_market: Pubkey,
    anchor_oracle_month: Pubkey,
    oracle_methodology_version: u64,
    product_manifest_root: [u8; 32],
    coverage_manifest_hash: [u8; 32],
    recipe_hash: [u8; 32],
    settlement_source_digest: [u8; 32],
    active_weight_manifest_hash: [u8; 32],
    security_cap_atoms: u64,
    signer_registry: Pubkey,
    signer_set: Pubkey,
    signer_set_version: u64,
    signer_set_hash: [u8; 32],
    settlement_ts: u64,
    settlement_price_atomic: u64,
    final_settlement_commitment: [u8; 32],
    submitted_by: Pubkey,
    finalized_slot: u64,
    sleeve: Pubkey,
    status: WriterSettlementGroupStatus,
    series_count: u8,
    reserved: [u8; 6],
    last_updated_slot: u64,
});

fixed_state_deserialize!(WriterSeriesBookV1, 8312, {
    is_initialized: bool,
    bump: u8,
    account_discriminator: [u8; 3],
    account_version: u8,
    sleeve: Pubkey,
    settlement_group: Pubkey,
    series_count: u8,
    max_series: u8,
    frozen: bool,
    reserved: [u8; 7],
    book_digest: [u8; 32],
    last_updated_slot: u64,
    records: Box<[WriterSeriesRecordV1; WRITER_SERIES_STORAGE_CAPACITY]>,
});

fixed_state_deserialize!(WriterAuctionV1, 1534, {
    is_initialized: bool,
    bump: u8,
    account_discriminator: [u8; 3],
    account_version: u8,
    sleeve: Pubkey,
    series_book: Pubkey,
    policy_snapshot: Pubkey,
    auction_nonce: u64,
    escrow: Pubkey,
    bid_index: Pubkey,
    fee_vault: Pubkey,
    policy_version: u64,
    scenario_set_hash: [u8; 32],
    risk_limit_hash: [u8; 32],
    reserve_vector_commitment: [u8; 32],
    reveal_hash: [u8; 32],
    revealed_nonce: [u8; 32],
    commit_slot: u64,
    bid_deadline_ts: u64,
    reveal_deadline_ts: u64,
    execute_deadline_ts: u64,
    bid_count: u16,
    planned_bid_count: u16,
    executed_bid_count: u16,
    refunded_bid_count: u16,
    planning_cursor: u16,
    reserved_0: [u8; 6],
    total_escrow_atoms: u64,
    accepted_premium_atoms: u64,
    accepted_fee_atoms: u64,
    accepted_contract_atoms: u64,
    refundable_atoms: u64,
    plan_digest: [u8; 32],
    status: WriterAuctionStatus,
    reserved_1: [u8; 7],
    last_updated_slot: u64,
    reserve_prices_atoms: [u64; WRITER_SERIES_STORAGE_CAPACITY],
    issue_caps_atoms: [u64; WRITER_SERIES_STORAGE_CAPACITY],
    planned_issue_atoms: [u64; WRITER_SERIES_STORAGE_CAPACITY],
    executed_issue_atoms: [u64; WRITER_SERIES_STORAGE_CAPACITY],
});

fixed_state_deserialize!(WriterBidIndexV1, 14936, {
    is_initialized: bool,
    bump: u8,
    account_discriminator: [u8; 3],
    account_version: u8,
    auction: Pubkey,
    bid_count: u16,
    planned_bid_count: u16,
    executed_bid_count: u16,
    refunded_bid_count: u16,
    planning_cursor: u16,
    rolling_digest: [u8; 32],
    last_updated_slot: u64,
    records: Box<[WriterBidIndexRecordV1; WRITER_BID_STORAGE_CAPACITY]>,
});

fixed_state_deserialize!(WriterBidV1, 230, {
    is_initialized: bool,
    bump: u8,
    account_discriminator: [u8; 3],
    account_version: u8,
    auction: Pubkey,
    bidder: Pubkey,
    refund_token_account: Pubkey,
    claim_destination: Pubkey,
    order_id: u64,
    series_index: u8,
    status: WriterBidStatus,
    delivery_mode: WriterBidDeliveryMode,
    reserved: [u8; 5],
    bid_price_per_contract_atoms: u64,
    requested_contract_atoms: u64,
    accepted_contract_atoms: u64,
    executed_contract_atoms: u64,
    escrowed_atoms: u64,
    premium_charged_atoms: u64,
    fee_charged_atoms: u64,
    refunded_atoms: u64,
    placed_slot: u64,
    last_updated_slot: u64,
});

fixed_state_deserialize!(WriterCloseRequestV1, 1088, {
    is_initialized: bool,
    bump: u8,
    account_discriminator: [u8; 3],
    account_version: u8,
    sleeve: Pubkey,
    owner: Pubkey,
    flat_escrow: Pubkey,
    flat_mint: Pubkey,
    request_nonce: u64,
    flat_amount_atoms: u64,
    minimum_withdrawal_atoms: u64,
    snapshot_asset_atoms: u64,
    snapshot_reserve_atoms: u64,
    snapshot_writer_principal_atoms: u64,
    snapshot_locked_primary_premium_atoms: u64,
    snapshot_flat_supply_atoms: u64,
    snapshot_security_exposure_atoms: u64,
    snapshot_policy_version: u64,
    snapshot_group_commitment: [u8; 32],
    snapshot_book_digest: [u8; 32],
    deadline_ts: u64,
    status: WriterCloseRequestStatus,
    series_count: u8,
    next_deposit_index: u8,
    next_cancel_index: u8,
    reserved: [u8; 6],
    required_claim_atoms: [u64; WRITER_SERIES_STORAGE_CAPACITY],
    deposited_claim_atoms: [u64; WRITER_SERIES_STORAGE_CAPACITY],
    snapshot_external_oi_atoms: [u64; WRITER_SERIES_STORAGE_CAPACITY],
    final_withdrawal_atoms: u64,
    finalized_slot: u64,
    last_updated_slot: u64,
});
